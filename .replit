# .replit
run = "install requirements.txt && python 0.py --host 0.0.0.0 --port 3000"

[env]
FLASK_APP = "0.py"
FLASK_ENV = "development"
PORT = "3000"

[nix]
channel = "stable-24_05"
packages = [
  "postgresql",
  "python310Full",
  "python310Packages.flask",
  "python310Packages.requests",
]

[deployment]
run = ["python", "0.py"]

[[ports]]
localPort = 3000
externalPort = 80
